# 设置工作区尺寸优化 QA

## 验证范围

- 优化前与优化后均由真实 `SettingsView` 经 `NSHostingView` 离屏渲染，不是重绘 mock。
- 默认窗口证据：`after-default-1473x768.png`。
- 同视口对照：`comparison-1487.png`、`comparison-1120.png`、`comparison-900.png`。
- 优化前/后原图与对照图保存在本机离屏渲染证据目录中，不随公开仓库发布。
- 测试机仍处于锁屏状态；本轮不把“窗口已前台可见”、悬停态和 VoiceOver 人工走查作为通过证据。

## 尺寸问题与修正

优化前存在两个明显断层：1080 pt 时突然加入 248 pt 二级栏，1320 pt 时两个导航栏又突然放大到 320/312 pt，详情区在窗口变宽时反而瞬间变窄。顶栏 88 pt、详情页头 184 pt 也与应用默认 1473 × 768 窗口不匹配。

修正后：

- 900 × 700：204 pt 主栏 + 696 pt 详情区，二级导航折叠为页头菜单；自动使用 56/104 pt 紧凑顶栏和页头。
- 1120 × 760：保持两栏，224 pt 主栏后仍有 896 pt 详情区，不再为了二级栏压缩表单。
- 1473 × 768 默认窗口：约 271/279/923 pt 三栏，顶栏 64 pt，页头 116 pt。
- 1480 pt 以上：两个导航栏上限为 272/280 pt，不再出现 1320 pt 断点跳变。三栏仅在剩余详情区至少 700 pt 时展开。

## 必检项

- 字体与排版：页标题使用 `.title2`，导航与说明使用语义字号；字号不再随窗口宽度跳变。
- 间距与布局：主栏行距为 8/5 pt（常规/紧凑），二级栏为 8/6 pt；应用默认高度下所有一级设置均可见。
- 表单控件：外观模式/密度控件收紧到 300/240 pt，标签列 220 pt，强调色点按 34 pt 点击区排列；三个视口均无横向滚动或截断。
- 站点上下文：204 pt 主栏下使用弹性 Picker + 图标式管理按钮，避免原先三行堆叠。
- 密度偏好：“紧凑”现在不仅改变原生控件大小，也改变设置工作区的顶栏、页头和导航行距。
- 响应式契约：测试覆盖 900、1120、1473、1487、1199/1200 断点、1319/1320 原跳变点和紧凑密度。

## 剩余 P3 / 证据边界

- 离屏窗口不是 key window，原生 Sidebar 选中底色为系统非活动灰；真实前台窗口会由当前强调色和活动状态决定。
- 锁屏阻止了本轮鼠标悬停、键盘焦点环和 VoiceOver 人工走查；代码级标签、标识、键盘返回和布局测试已覆盖。

final result: passed

---

# 顶部工具栏重排 QA（2026-08-31）

## 范围与设计取舍

根据用户提供的顶部栏效果图，在现有 macOS 原生工具栏中实现功能分组；不改造效果图中的正文、纸张背景或整个窗口外观。

- 左侧：独立侧栏开关、站点切换、动态检查状态。
- 中部：命令搜索入口，以及当前文章的实时字数与预计阅读时间。
- 右侧：双预览按钮、任务中心、AI 助手、Inspector、设置、蓝色“准备发布”。
- 保留原生窗口控制、系统工具栏高度与现有语义字体；保留产品已有的设置入口，因此不是效果图的逐像素复制。
- 保留独立的原生工具栏按钮宿主，避免将整个操作区合并为一个可访问性元素。

## 视觉证据与比较条件

- 参考图：`/Users/7c96/Desktop/top_bar_redesign_mockup_1788095717985.jpg`，1376 × 768。
- 最终实际应用窗口：`/private/tmp/repopress-topbar-native-v3.jpeg`，1628 × 761，由 Computer Use 获取。
- 同一比较输入：`/private/tmp/repopress-topbar-comparison-v3-full.png` 与 `repopress-topbar-comparison-v3-focus.png`，已打开检查。
- 全图比较保持图片纵横比；聚焦比较使用参考图顶部栏区域和实际窗口前 1240 px 的工具栏区域，后者覆盖所有持久操作按钮，不包含右侧空白。参考条与实际条高度不同，不作纵向拉伸。
- 请求的窗口内容尺寸为 1240 × 760，但实际窗口恢复后为 1628 × 761。该证据用于顶部栏顺序、内容和可见性检查，不作为相同视口下整个界面像素一致的证明。
- 实际截图使用演示站点和文章，状态为“1 个阻断项”，不是参考图的“检查通过”；这是动态状态，不硬编码绿色通过。非活动窗口的交通灯颜色由系统决定。
- 自动离屏截图曾出现 Inspector 材质伪影，未将其作为最终整窗视觉证据；最终采用实际窗口截图。

## 迭代与五项检查

第一轮发现搜索区域偏窄、阅读时间缺少“阅读”后缀、预览按钮缺少共同分组表面。第二轮将展开搜索宽度调整为 340 pt，补充本地化统计文案与数字分组，并增加轻量预览分组背景；第三轮重新构建应用并检查真实窗口。

1. 字体与排版：沿用现有原生工具栏语义字体；搜索提示、统计和快捷键保持主次层级。参考图较高、字号较大的概念工具栏未直接覆盖系统窗口密度。
2. 间距与布局：控件顺序与功能分组明确，最终窗口中顶部操作无重叠或被遮挡。搜索按展开、紧凑、最小三档使用 340/216/32 pt；断点有单元测试覆盖。
3. 颜色与材质：发布使用蓝色突出，检查状态由真实结果驱动；选中开关与预览分组使用现有系统语义色和轻量表面，不以固定背景图仿制材质。
4. 图标与资源：使用原生 SF Symbols；无手绘替代资源。统计本地化使用标准 Foundation API，默认 Bundle.main，不依赖 SwiftPM 绝对构建路径，也不在视图更新路径手动读取文件。
5. 文案与内容：搜索仍打开现有命令面板；字数与阅读时间来自当前编辑器，未加载时不伪造零字。草稿和编辑器身份隔离，过期统计回调不能覆盖当前文章。

结论：顶部栏范围内未发现需要继续修复的 P0/P1/P2 视觉问题。系统高度、演示数据状态、保留设置入口和窗口宽度差异均在上述适配边界内。

## 验证结果

- `swift build --scratch-path /private/tmp/repopress-topbar-integrate-build --target PersonalSitePublisherMac`：通过。
- `swift test --scratch-path /private/tmp/repopress-topbar-integrate-build --filter 'Workspace(TopBarPresentation|SidebarVisibilityPolicy|ToolbarEditorContext|ResponsiveLayoutSnapshot)Tests'`：18 项通过，0 失败；最终使用同一测试产物 `--skip-build` 再次确认通过。
- 最新本地化资源边界修正后，`WorkspaceTopBarPresentationTests`：6 项重新编译运行通过；随后重新构建并打包最终 QA 应用。
- `./script/check_accessibility.sh`：源码契约检查通过。已更新顶部控件标识和 XCUI 顺序断言；这不等于完整 XCUI 或 VoiceOver 验收。
- 实际交互：侧栏按钮可隐藏并恢复侧栏，可访问性值同步变化；命令搜索按钮可打开面板并聚焦输入框。
- 实际 AX 树：顶部控件保留独立标识，当前文章统计可读取。
- 最终 QA 应用：`/private/tmp/repopress-topbar-qa-20260831/RepoPress Studio.app`，使用隔离的演示持久化目录与测试 bundle identifier；本机 ad hoc 签名校验通过，不代表分发签名或公证通过。
- 本次改动的 `git diff --check` 与更新后 UI 测试源文件解析：通过。未提交或推送；保留并发中的其他工作区改动。

## 剩余验证边界

深色模式、窄窗口实际交互、完整键盘流程、完整 XCUI、VoiceOver、浏览器预览端到端与真实发布流程本轮未执行。响应式与状态测试不能代替这些运行时验收。

final result: passed
