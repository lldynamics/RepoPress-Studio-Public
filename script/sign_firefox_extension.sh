#!/usr/bin/env bash
set -euo pipefail

echo "Firefox release signing is retired for the current product channels." >&2
echo "Restore it only through a separately reviewed, isolated dependency workflow." >&2
exit 2
